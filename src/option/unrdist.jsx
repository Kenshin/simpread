console.log( "===== simpread option unread list load =====" )

export default class Unrdist extends React.Component {
    render() {
        return (
            <div>
                Unread list
            </div>
        )
    }
}